---
name: yamllint
"on":
  pull_request:
    branches: [onboard-to-task-repo-shared-ci]
  merge_group:
    types: [checks_requested]
jobs:
  yamllint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Lint YAML files
        run: yamllint .
